input {
    file {
        path => "/logs/*"
        start_position => "beginning"
        codec => "json"
    }
}

output {
  elasticsearch {
        index => "codelytv"
        hosts => ["codelytv-cqrs_ddd_java_example-elasticsearch:9200"]
    }
}
